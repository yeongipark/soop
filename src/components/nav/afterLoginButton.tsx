"use client";
import { useState, useEffect } from "react";

// 클라이언트 컴포넌트의 범위를 줄이기 위해 따로 컴포넌트로 분리
export default function AfterLoginButton() {
  // recoil로 로그인 상태 전역으로 저장하기
  // const [isLogin,setIsLogin] = useState<Boolean>(false);
}
